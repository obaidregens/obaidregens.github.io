_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
vendor/
.bundle/
Gemfile.lock
.DS_Store

# private research / anonymized account archive — never publish
.research/

# linkedin contact data — names, headlines and profile slugs of third
# parties who never consented to being published. never commit.
data/linkedin-*.json
data/linkedin-invite-triage.md

# thread drafts, working files
thread-*.md
thread-*.txt
data/thread.json

# superseded by data/startup-school-2026-events.json
/startup-school-2026-events.md

# schedule-compare: attendee email lists (raw PII) + worker secrets. never commit.
# repo is public; the hashed allowlist is a membership oracle, so it is bundled
# into the worker at deploy from this local file but never committed.
data/attendees*
**/attendees*.csv
**/attendees*.json
worker/schedule/allowlist.json
*ss26-secrets*
worker/schedule/.dev.vars

# python bytecode
__pycache__/
